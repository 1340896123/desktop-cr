# 2026-08-20

## UI 数据链路真实性审查(桌面客户端)
- 用 Explore subagent 从 UI(tsx)→ src/services/*.ts → Tauri invoke → src-tauri/src/*.rs 审查数据真实性。
- 结论:Windows + 真实 Tauri 环境下 UI 数据均为真实来源(真实 DXGI 抓屏 / 真实 LAN TCP 协议 / 真实 IDD 驱动 / 真实 Win32 剪贴板·输入 / 真实 WASAPI 音频 / 真实信令中继 HTTP)。前端调用名与 main.rs 注册名一致,事件发射点(`remote-frame`/`capture-frame`/`connection-state`/`host-state`/`clipboard-synced` 等)真实存在。
- 纯浏览器 `npm run dev`:每个 service 在 `!isTauri()` 分支返回硬编码 mock/空/noop,UI 全为伪造,但已用"模拟模式"徽标 + console.warn 显式标注。
- 两处"假成功"风险点(需警惕):
  1. `src/services/connection.ts:54-58` — 非 Tauri 下 `connectToDevice` 直接返回 `{connected:true}`(假已连接)。
  2. `src-tauri/src/hbb_client.rs:994-998` — 非 Windows `start_host` 推送假 `host-state {running:true}`(日志明示"非 Windows 模拟")。
- 非 Windows 平台 capture 推送 `simulated:true` 动画帧,bench 失败回退也标 `synthetic:true`(透明标记)。

## 修正非 Windows `start_host` 假成功
- 文件 `src-tauri/src/hbb_client.rs` 非 Windows `#[cfg]` 分支(原 994-1005):原伪造 `host-state {running:true}` 返回 `Ok`,已改为返回 `Err("被控端(host)仅 Windows 平台支持,当前平台不可用")` 并广播真实 `running:false`,与虚拟屏 `Err` 处理一致。
- 同时把 `port` 写入日志、`app` 用于 emit,避免非 Windows 编译期未使用变量 warning(满足 cargo check 零警告)。
- `cargo check` 通过(零警告)。审查报告 `docs/ui-linkage-review-report.md` 对应 D8/G5/风险表已标记"已修正"。
